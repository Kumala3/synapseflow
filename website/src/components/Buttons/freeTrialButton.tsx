import { Button } from "@nextui-org/react";

export function FreeTrialButton() {
    return (
        <Button size="lg" variant="shadow" color="primary" radius="md">
            Try for free
        </Button>
    );
}
